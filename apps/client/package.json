{
  "name": "@/client",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "wrangler pages dev build --port 3000",
    "dev": "vite dev",
    "build": "NODE_ENV=production bun utils/copyHeaders.ts && vite build && bun utils/deleteHeaders.ts",
    "build/staging": "NODE_ENV=staging bun utils/copyHeaders.ts && vite build && bun utils/deleteHeaders.ts",
    "build/dev": "NODE_ENV=development bun utils/copyHeaders.ts && vite build && bun utils/deleteHeaders.ts",
    "preview": "vite preview",
    "format": "cd ../../ && bun format"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.8.5",
    "@sveltejs/vite-plugin-svelte": "^4.0.2",
    "svelte": "^5.2.9",
    "svelte-turnstile": "^0.9.0",
    "typescript": "^5.7.2",
    "vite": "^5.4.11",
    "wrangler": "^3.91.0",
    "zod": "^3.23.8"
  },
  "dependencies": {
    "@/public": "workspace:*",
    "@discord/embedded-app-sdk": "workspace:*",
    "@msgpack/msgpack": "^3.0.0-beta2",
    "@sveltejs/adapter-static": "^3.0.6",
    "dompurify": "^3.2.1",
    "eventemitter3": "^5.0.1",
    "marked": "^15.0.2"
  }
}
