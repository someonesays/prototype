{
  "name": "@/server",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "start": "NODE_ENV=production TZ=UTC bun .",
    "dev": "NODE_ENV=development TZ=UTC bun --watch .",
    "format": "cd ../../ && bun format"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@/db": "workspace:*",
    "@/env": "workspace:*",
    "@/public": "workspace:*",
    "@/utils": "workspace:*",
    "@hono/zod-validator": "^0.4.1",
    "@msgpack/msgpack": "^3.0.0-beta2",
    "hono": "^4.6.10",
    "zod": "^3.23.8"
  }
}
