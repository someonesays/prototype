{
  "name": "@/server",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "start": "NODE_ENV=production TZ=UTC bun --env-file=../../.env .",
    "dev": "NODE_ENV=development TZ=UTC bun --env-file=../../.env --watch .",
    "format": "cd ../../ && bun format"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@/db": "workspace:*",
    "@/env": "workspace:*",
    "@/public": "workspace:*",
    "@/utils": "workspace:*",
    "@msgpack/msgpack": "^3.0.0-beta2",
    "hono": "^4.5.10"
  }
}
